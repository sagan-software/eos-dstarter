#include "../contract.hpp"

namespace dapp
{

void system::pubproject(
    const name account,
    const name draft_name,
    const name live_name)
{
    // Make sure a project exists with that slug

    // Make sure we have authority over the project

    // Make sure the project is a draft

    // Title should be 1-60 characters

    // Subtitle should be 0-135 characters

    // Category must be valid

    // Subcategory must be valid

    // Image must be empty or a valid IPFS hash

    // Video must be empty or a valid IPFS hash

    // Story must be empty or a valid IPFS hash

    // Funding goal must be greater than 0

    // Funding goal must be in the core symbol (EOS, TLOS, etc.)
}

} // namespace dapp